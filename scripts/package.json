{
  "name": "database-scripts",
  "version": "1.0.0",
  "description": "Database seeding and management scripts for DrugOrPokemon app",
  "scripts": {
    "seed:facts": "npx ts-node seed-facts-questions.ts",
    "clear:facts": "npx ts-node clear-facts-questions.ts",
    "seed:facts-fresh": "npm run clear:facts && npm run seed:facts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
